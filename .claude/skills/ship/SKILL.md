---
name: ship
description: Claude adapter for the canonical governed Governada ship loop
---

This is a Claude Code adapter. The canonical provider-agnostic ship skill is `.agents/skills/ship/SKILL.md`, and the canonical repo policy is `AGENTS.md`.

Use the repo policy from those files. Do not restate or revive archived broker, Keychain, LaunchAgent, `github:*`, `pr:ready`, or `pr:merge` lanes.
